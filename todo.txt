- Make import-data support 2019 CVRExport (no Election manifest, single Cvr json file)
- Can we identify how many ballots aren't included when a filter is applied?
- Allow clicking on a candidate row to apply them as a filter?
- Separate races by type? Group presidential primaries, house, state leg, props, etc?
